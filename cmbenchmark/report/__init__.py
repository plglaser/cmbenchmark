"""Derived report builders.

This package contains small, dimension-focused builders which transform raw measures
into a stable, UI-ready report payload for report jobs (`/api/report-jobs/{id}`).
"""
